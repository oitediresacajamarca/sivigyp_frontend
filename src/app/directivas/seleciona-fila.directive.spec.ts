import { SelecionaFilaDirective } from './seleciona-fila.directive';

describe('SelecionaFilaDirective', () => {
  it('should create an instance', () => {
    const directive = new SelecionaFilaDirective();
    expect(directive).toBeTruthy();
  });
});
