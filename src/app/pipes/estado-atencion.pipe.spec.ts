import { EstadoAtencionPipe } from './estado-atencion.pipe';

describe('EstadoAtencionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoAtencionPipe();
    expect(pipe).toBeTruthy();
  });
});
