import { TipoPartoPipe } from './tipo-parto.pipe';

describe('TipoPartoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPartoPipe();
    expect(pipe).toBeTruthy();
  });
});
