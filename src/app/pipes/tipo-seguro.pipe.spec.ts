import { TipoSeguroPipe } from './tipo-seguro.pipe';

describe('TipoSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
