import { TipoMetodoPpffPipe } from './tipo-metodo-ppff.pipe';

describe('TipoMetodoPpffPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoMetodoPpffPipe();
    expect(pipe).toBeTruthy();
  });
});
